<section class="value-prop section-padding">
    <div class="container fade-up">
        <div class="value-prop-grid" id="value-prop-container">
            <div class="value-prop-text text-center">
                <h2>Lo que está en mi mano</h2>
                <p>
                    Te muevo para que elijas lo que te importa y dejes de
                    posponer tu VIDA.
                </p>
                <p>
                    Trabajo con herramientas para pensar, hablar y decidir
                    mejor. Para cambiar la forma en la que miras y, por tanto,
                    la forma en la que vives.
                </p>
                <p class="highlight" style="margin-top: 1.5rem;">
                    Comparto mi experiencia y mis preguntas para que conectes
                    con tu historia y encuentres tus propias respuestas.
                </p>
                <div class="el-resto-container mt-4">
                    <span class="el-resto-line"></span>
                    <h3 class="giant-text-visual">
                        EL RESTO,<br /><span class="es-tuyo">ES TUYO</span>
                    </h3>
                </div>
            </div>
            <div class="value-prop-image">
                <div class="image-placeholder">
                    <img
                        src="/assets/images/PRINT_AnaAlbiol_EA-37.webp"
                        alt="Ana Albiol"
                        loading="lazy"
                        class="placeholder-img"
                        style="object-position: center 30%;"
                    />
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    import { inView, animate, stagger } from "motion";

    const container = document.getElementById("value-prop-container");
    if (container) {
        inView(container, () => {
            animate(
                Array.from(container.children),
                { opacity: [0, 1], y: [30, 0] },
                { duration: 0.8, delay: stagger(0.2) },
            );
        });
    }
</script>

<style>
    .el-resto-container {
        margin-top: 3rem;
        position: relative;
        padding-left: 2rem;
        border-left: 4px solid var(--color-primary);
        text-align: left;
    }

    .giant-text-visual {
        font-family: var(--font-heading);
        font-size: 3.2rem;
        line-height: 1;
        color: var(--color-primary);
        text-transform: uppercase;
        letter-spacing: -1px;
        margin: 0;
    }

    .giant-text-visual .es-tuyo {
        font-style: italic;
        font-weight: 300;
        color: var(--color-text);
        font-size: 2.8rem;
    }

    @media (min-width: 900px) {
        .giant-text-visual {
            font-size: 5rem;
        }
        .giant-text-visual .es-tuyo {
            font-size: 4rem;
        }
    }

    .value-prop-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 4rem;
        align-items: center;
    }

    @media (min-width: 900px) {
        .value-prop-grid {
            grid-template-columns: 1.2fr 0.8fr;
        }
        .value-prop-text.text-center {
            text-align: left;
        }
    }

    #value-prop-container > * {
        opacity: 0;
    }

    .image-placeholder {
        background-color: rgba(
            99,
            53,
            54,
            0.03
        ); /* Extremely light primary variant */
        border: 1px dashed rgba(99, 53, 54, 0.3);
        border-radius: var(--radius-md);
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 450px;
        position: relative;
        overflow: hidden;
        width: 100%;
    }

    .image-placeholder span {
        color: var(--color-primary);
        font-family: var(--font-subtitle);
        opacity: 0.5;
        text-align: center;
        padding: 2rem;
        font-size: 0.9rem;
        letter-spacing: 0.5px;
    }

    .placeholder-img {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0; /* Hidden when src is empty */
        transition: opacity 0.3s ease;
    }

    .placeholder-img:not([src=""]) {
        opacity: 1; /* Revealed when image is loaded */
    }
</style>
